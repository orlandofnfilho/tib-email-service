CREATE OR ALTER PROCEDURE SEL_EMAIL_REMETENTE
(
    @REMETENTE NVARCHAR(255)
)
AS
BEGIN
SET NOCOUNT ON  
SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED 
	
	SELECT 
	ID,
	REMETENTE,
	DESTINATARIO,
	CONTEUDO,
	DATA_ENVIO,
	ERRO
	FROM TB_EMAIL TE 
	WHERE TE.REMETENTE = @REMETENTE
	
END

